<div class="row">
  <div class="col-md-12 actions">
    <div class="page-header">
      <%= link_to '<i class="fa fa-chevron-left"></i> Volver'.html_safe, admin_consumable_supplies_path, :class => 'btn btn-default btn-lg pull-right' %>
      <h2><%= @page_title %></h2>
    </div>
  </div>
</div>
<div class="row">
  <div class="col-md-12">

    <dl>
      <dt>Marca</dt>
      <dd><%= @consumable_supply.brand %></dd>
      <dt>Modelo</dt>
      <dd><%= @consumable_supply.model %></dd>
      <dt>Categoría</dt>
      <dd><%= @consumable_supply.category %></dd>
      <dt>Precio</dt>
      <dd><%= money_for @consumable_supply.price %></dd>
      <dt>Stock actual</dt>
      <dd><%= @consumable_supply.supply_items.available.count %> <%= link_to '<i class="fa fa-plus"></i> Agregar unidades'.html_safe, '#', class: 'add-units', style: 'font-size: 10px;', data: {supply_id: @consumable_supply.id} %></dd>
      <dt>Stock consumido</dt>
      <dd><%= @consumable_supply.supply_items.unavailable.count %></dd>
    </dl>

  </div>
</div>


<div id="add-units-modal"></div>

<script type="text/javascript">
  $(document).ready(function(){
    $('.add-units').on('click', function(){
      var supply_id = $(this).data('supply-id');
      var url = '<%= add_units_modal_admin_consumable_supplies_path(format: 'js') %>?supply_id='+ supply_id;
      $.getScript(url);
    });
  });
</script>